// Shared public view — same ResultsPage component, routed under /share/:shareId
// The ResultsPage component handles both cases via the `paramShareId` check.
export { default } from './ResultsPage';
